This folder contains everything needed to run the Shiny app in full.

Some parts of the code in `app.R` and `test.py` may require modification to local file paths, such as model or data file references.
